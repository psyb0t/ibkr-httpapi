# IBKR credentials for IB Gateway auto-login.
# Copy to .env.ibkr, fill in, never commit.
# Compose loads this via `env_file:` on the ibgateway service.

TWS_USERID=your_ibkr_username
TWS_PASSWORD=your_ibkr_password
TRADING_MODE=paper
# READ_ONLY_API=yes   # uncomment to block trading API entirely
